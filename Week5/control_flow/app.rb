students = 9

# If else
if students > 12
  puts "That's a big group!"
elsif students < 5
  puts "That's a small group..."
else
  puts "That seems about right."
end

# If then
val = 1

if val === 1 then puts "1" else "not 1" end

# Ternary
num1 = 2
num2 = 17

puts num2 > num1 ? "Num2 is larger" : num2 === num1 ? "Equal" : "Num1 is larger"

# Unless
this = 4
that = 6

unless this === that
  puts "They are not equal"
end
